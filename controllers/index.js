const userController = require('./modules/userController')
const categoryController = require('./modules/categoryController')
const recordController = require('./modules/recordController')
module.exports = {
  userController,
  recordController,
  categoryController
}
